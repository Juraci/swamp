#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'swamp'

swamp = Swamp::Initializer.new(STDOUT)
swamp.run

while input = gets.chomp
  if input == 'exit'
    exit
  end
  swamp.scan(input)
end

